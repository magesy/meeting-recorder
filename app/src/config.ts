/**
 * Application configuration
 */
export const CONFIG = {
  // Replace with your machine's local IP (e.g., 'http://192.168.1.50:3000') 
  // to test on a physical device.
  BACKEND_URL: 'http://localhost:3000',
  
  // Maximum recording time in seconds (optional safety limit)
  MAX_RECORDING_DURATION: 3600, // 1 hour
};
