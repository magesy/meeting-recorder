services:
  - type: web
    name: meeting-recorder-backend
    env: docker
    plan: free
    region: oregon
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
      - key: GROQ_API_KEY
        sync: false
      - key: UPLOAD_DIR
        value: uploads
